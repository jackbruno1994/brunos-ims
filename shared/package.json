{
  "name": "@brunos-ims/shared",
  "version": "1.0.0",
  "description": "Shared types and utilities for Bruno's IMS",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  },
  "peerDependencies": {
    "mongodb": "^5.0.0"
  }
}